import config from './server.config.js';
import connectDB from './db.config.js';

// Export all configurations from a single point for easy access throughout the application.
export { config, connectDB };
